{
  "name": "quadro-scrap",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run index.ts",
    "get-classes": "bun run index.ts --get-classes-only",
    "scrape-classes": "bun run index.ts --scrape-only",
    "scrape-skip": "bun run index.ts --skip-get-classes"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "puppeteer": "^24.15.0"
  }
}